using UnityEngine;

public class GraphUtils
{
	public static bool IsInSector(Vector3 origin, Vector3 focus, float width, float radius, Vector3 target)
	{
		return false;
	}

	public static bool IsInRect(Vector3 origin, Vector3 focus, float width, float height, Vector3 targetPos)
	{
		return false;
	}

	public static bool IsInCircle(Vector3 origin, float radius, Vector3 target)
	{
		return false;
	}

	public static bool IsInRange(Entity target1, Entity target2, float radius)
	{
		return false;
	}
}
